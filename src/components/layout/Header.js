import React, { memo, useEffect, useState } from "react";


const Headers= memo((props) => {
 

    useEffect(() => {
     
    }, []);

    
    return (
        <React.Fragment>
          <h1>Header</h1>
        </React.Fragment>
    );
});

export default Headers;
